fn main() -> i32 {
    'entry: {
        %15: [[i32; 2]; 4] = {0};
        %16: [[i32; 2]; 4] = {1, 2, 3, 4, 5, 6, 7, 8};
        %0: &mut[i32] = &mut %16[3];
        %1: &i32 = & %0[1];
        %2: i32 = * %1;
        %3: &mut[i32] = &mut %16[0];
        %4: &i32 = & %3[0];
        %5: i32 = * %4;
        %6: i32 = %2 + %5;
        %8: &i32 = & %3[1];
        %9: i32 = * %8;
        %10: i32 = %6 + %9;
        %11: &mut[i32] = &mut %15[2];
        %12: &i32 = & %11[0];
        %13: i32 = * %12;
        %14: i32 = %10 + %13;
        return %14;
    }
}
