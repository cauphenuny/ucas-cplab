fn main() -> i32 {
    let ref y_0: [i32; 2];
    let ref a_0: [i32; 1000];
    'entry: {
        @y_0 <- {2, 3};
        @a_0 <- {0};
        %0: &mut i32 = &mut @a_0[999];
        %0 <- 41;
        %1: &i32 = & @a_0[999];
        %2: i32 = * (&i32)%1;
        %3: i32 = %2 + 1;
        %4: &i32 = & @y_0[0];
        %5: i32 = * (&i32)%4;
        %6: i32 = %3 + %5;
        %7: &i32 = & @y_0[1];
        %8: i32 = * (&i32)%7;
        %9: i32 = %6 + %8;
        return %9;
    }
}
