fn main() -> i32 {
    let ref a_0: [[i32; 2]; 4];
    let ref e_0: [[i32; 2]; 4];
    'entry: {
        @a_0 <- {0};
        @e_0 <- {1, 2, 3, 4, 5, 6, 7, 8};
        %0: &mut[i32] = &mut @e_0[3];
        %1: &i32 = & %0[1];
        %2: i32 = * (&i32)%1;
        %3: &mut[i32] = &mut @e_0[0];
        %4: &i32 = & %3[0];
        %5: i32 = * (&i32)%4;
        %6: i32 = %2 + %5;
        %7: &i32 = & %3[1];
        %8: i32 = * (&i32)%7;
        %9: i32 = %6 + %8;
        %10: &mut[i32] = &mut @a_0[2];
        %11: &i32 = & %10[0];
        %12: i32 = * (&i32)%11;
        %13: i32 = %9 + %12;
        return %13;
    }
}
