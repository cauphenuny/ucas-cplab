fn main() -> i32 {
    let mut a_0: [[i32; 2]; 4];
    let mut e_0: [[i32; 2]; 4];
    'entry: {
        @a_0: [[i32; 2]; 4] = {0};
        @e_0: [[i32; 2]; 4] = {1, 2, 3, 4, 5, 6, 7, 8};
        %0: &mut[i32] = &mut @e_0[3];
        %1: &i32 = & %0[1];
        %2: i32 = * %1;
        %3: &mut[i32] = &mut @e_0[0];
        %4: &i32 = & %3[0];
        %5: i32 = * %4;
        %6: i32 = %2 + %5;
        %7: &mut[i32] = &mut @e_0[0];
        %8: &i32 = & %7[1];
        %9: i32 = * %8;
        %10: i32 = %6 + %9;
        %11: &mut[i32] = &mut @a_0[2];
        %12: &i32 = & %11[0];
        %13: i32 = * %12;
        %14: i32 = %10 + %13;
        return %14;
    }
}
